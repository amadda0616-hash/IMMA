open source: https://www.youtube.com/watch?v=WL2fqiK7zlE
----------------------------------------------------------------------------------------------

gpt4 torbo

a, b, c, d(없음) 이라는 선택지가 있는데

주어진 스케치와 텍스트 설명이 있으면 적중률이 100% 였다.
그런데 텍스트를 삭제하니 53%로 감소.

아무것도 아닐 수도 있음. 이라는 선택지를 고르는 옵션을 삭제하고
선택지를 강제로 고르게 했더니. 70%로 상승.

어쨌든 무조건 정답이 있어라고 강제하면 성능이 올랐다. (결과론적)

차트가 안 쓰였을 수도... 있다?

특정해야 하는 것에 대한 직관적인 설명+구체적인 설명인지는 파악이 안 됨.
특정 위치에서 보는 시선에 대한 것인지도 파악이 안됨.
하지만 거기에 + 추가적인 요구사항과 명칭에 대해 고려해달라는 
프롬프트를 넣으면 결과가 달라짐.

-> 단순하게 유튜브로만 봐서는 파악할 수는 없지만, 직관적인 설명or구체적인설명or  [ 특정위치에서 보는 시선인지 아닌지를 고려] 해서 
(일종의 갈래 형식으로 나눠서 실험하고 대조하면 똑같이 따라갈 수는 있을 거로 보임)
거기에 + 추가적인 요구사항과 명칭에 대해 고려해달라는 
프롬프트를 넣으면 결과가 달라짐.

1번 정도는 다시 생각해달라 하면 성능이 올라간다. 그 이상은 의미가 없다

특정 규칙이 있다면 gpt4 torbo는 그에 맞춰서만 움직이려고 할 정도로 
신중하다. (온톨로지 [같은 규칙이 제대로 세워져 있으면 성능이 올라간다->시너지 있음]
 
lava 1.6 34b는 입력 제한이 있어서 성능이 안 나오는 것일 뿐이다.

2d -> 3d로 전환하고, 추론 부분에서 gpt4 붙이기.
엔지니어의 요구에 맞는 의미 있는 데이터셋.

결론: 
1. vlm이 이미지를 이해하면 파이썬라이브러리가 계산 담당
OpenCV
Shapely
NumPy
SciPy
CAD parser

2. vlm의 api를 기존 도구와 결합.
도면 이미지
→ YOLO(기호 탐지)
→ OCR(텍스트 읽기)
→ VLM(문맥 이해)
→ Python 규칙엔진(검증)
→ JSON
→ DB

-시동이 vlm이 먼저일 수도 있고, 기존 도구가 먼저일 수도 있으나 비용상 vlm이 후속.
조건부를 걸어서, 정말 필요할 때만 vlm을 호출해서 사용한다.  

ps. 확정인지 아닌지 확인해보기 전에는 모르니 갈래 중 1개로 둘 것.



